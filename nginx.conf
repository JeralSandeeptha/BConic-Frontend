server {
  listen 80;

  root   /usr/share/nginx/html;
  index  index.html index.htm;

  location / {
    try_files $uri /index.html;
  }

  # Proxy API requests to backend_container
  location /api/v1 {
    proxy_pass http://127.0.0.1:5000/;
  }
}
